<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.picodiploma.courseon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Chatroom"></activity>
        <activity android:name=".LihatLogActivity" />
        <activity android:name=".LihatLogGuruActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".MatpelActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".TambahJadwal" />
        <activity android:name=".MainActivityGuru" />
        <activity android:name=".Login" />
        <activity android:name=".Register">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>